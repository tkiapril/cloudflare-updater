- token: CloudFlare API Token with #dns_records:edit permissions from https://dash.cloudflare.com/profile/api-tokens
  targets:
  # get these from lookup_id.py
  - id: record id
    zone_id: zone id
    interface: interface name
